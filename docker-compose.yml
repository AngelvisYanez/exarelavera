version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
    environment:
      - PHP_ERROR_REPORTING=E_ALL & ~E_DEPRECATED & ~E_NOTICE
      - PHP_DISPLAY_ERRORS=On
